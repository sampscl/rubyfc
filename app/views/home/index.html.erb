<%# app/views/home/index.html.erb %>
<%= react_component 'Home', { data:
  {
    games: @games,
    fleets: @fleets,
    leagues: @leagues,
    missions: @missions,
    tournaments: @tournaments,
    users: @users,
  }
} %>
